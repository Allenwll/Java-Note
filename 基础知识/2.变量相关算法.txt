变量相关算法

	自增
	
		自增是一个变量在原有值的基础上再增加指定值
		
		i=i+1 等效于 i+=1;
		++i 先加1 再自增  i++ 先取原值再后加1
		
	自减
	    i=i-1 等效于i-=1;
		自减是一个变量在原有值的基础上再减去指定值
		--i 先减1 再自减  i-- 先取原值再后减1
		
	自乘
	
		自乘是一个变量在原有值的基础上再乘以指定值
		 i=i*1 等效于i*=1;
		 
	自除
	
		i=i/1 等效于i/=1;
		自除是一个变量在原有值的基础上再除以指定值